name: build messenger ui

on:
  pull_request:
    branches: [ master ]
    paths:
      - messenger-ui/**
      - 'docker-compose.yml'
      - .github/workflows/*-messenger-ui.yml
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./messenger-ui

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
#
#      - name: Set up Node.js
#        uses: actions/setup-node@v1
#
#      - name: Install dependencies
#        run: npm install
#
#      - name: build
#        run: npm run build

      - name: DELETE ME
        run: |
          ssh -oStrictHostKeyChecking=no ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_IP }} ${{ secrets.VPS_USER_PASSWORD }}<<EOF
          mkdir -p messenger-app
          EOF